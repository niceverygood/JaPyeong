{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd mobile && npm install",
  "buildCommand": "cd mobile && npm run build:web && cd .. && rm -rf web/app && mkdir -p web/app && cp -R mobile/dist/. web/app/",
  "outputDirectory": "web",
  "framework": null
}
