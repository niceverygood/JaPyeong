services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: japyeong-postgres
    environment:
      POSTGRES_USER: japyeong
      POSTGRES_PASSWORD: japyeong
      POSTGRES_DB: japyeong
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U japyeong"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: japyeong-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
  redisdata:
