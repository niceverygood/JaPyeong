# 자평 — 알려진 이슈 (점검 세션 2026-05-22)

우선순위: **P0** 지금 막힘 / **P1** 곧 막힘 / **P2** 개선.
"자문위원" = 명리 도메인 확정이 필요해 코드만으로 못 푸는 것.

---

## P0 — 지금 막힘

### P0-1. 검증 케이스 0건 → 출시 게이트 차단
- **현상**: `validation_cases/` 실제 케이스 0건. `RUN_GATE_CHECK=1` FAIL(기준 100건).
- **연관**: 전 엔진 모듈(특히 pillars). 하네스(`tests/engine/test_harness.py`)는 정상.
- **영향**: 8자가 스팟체크 5건은 맞으나, **자문위원 승인된 정답지가 없어 "검증됨"이라 말할 수 없음.** 정식 출시 불가.
- **해결 주체**: **자문위원 필요** (출처 있는 케이스 수집·승인). 코드로는 못 푼다.

### P0-2. 8자가 "스팟체크 통과"일 뿐 "검증 완료"는 아님
- **현상**: `scripts/spot_check.py` 5/5 일치(년주 독립표·일주 anchor 독립계산·시주 五鼠遁 독립·월주 sxtwl). 그러나 5건은 모두 longitude=None(진태양시 off)·3~11월(입춘/세수 경계 회피)·정시.
- **연관**: pillars + solar_time + jeolgi.
- **영향**: **경계 케이스 미검증** — 진태양시로 일/시 경계를 넘는 출생, 입춘 전후 며칠, 자시(23~01시), 음력 윤달. 이 구간이 사주 8자를 바꾸는데 스팟체크에 없음.
- **해결 주체**: 일부 코드(경계 단위 테스트 추가 가능) + **자문위원**(경계 정책 항목 1·2·6·12 확정).

---

## P1 — 곧 막힘

### P1-1. 🔴 strength/geokguk/yongsin 미착수 = 핵심 가치 미구현
- **현상**: 신강신약·격국·용신 모듈 자체가 없음. `analyze`는 8자·십성·오행·관계·대운까지만.
- **연관**: policy 항목 7(geokguk_priority)·8(yongsin_method) — default만 박혀 있고 소비 코드 없음.
- **영향**: 자평의 "자문" 핵심(격국·용신)이 아직 산출 안 됨. LLM 자문 입력 JSON이 불완전.
- **해결 주체**: **자문위원 필요** (정책 7·8 확정 + 검증 케이스 50건+). 로드맵상 착수 전제 미충족.

### P1-2. 전 정책이 "잠정 default" — 확정 0건
- **현상**: `policy.confirmed == {}`. 12개 항목 모두 자문위원 미확정.
- **연관**: myeongri-policy.md 전 항목 🟡.
- **영향**: pillars/daewoon/sewoon 결과가 미확정 가정 위에 있음. 정책이 바뀌면 8자/대운수가 달라질 수 있음(특히 자시·진태양시 균시차).
- **해결 주체**: **자문위원 필요**.

### P1-3. 진태양시·음력 경로가 스팟체크 무관 영역
- **현상**: spot_check는 longitude=None. 진태양시(WITH_EOT) 보정과 음력→양력 변환은 단위 테스트로만 존재.
- **연관**: solar_time, pillars(음력).
- **영향**: 실제 `analyze`는 진태양시를 켜고 동작 → 스팟체크가 커버 못한 경로가 운영 기본값.
- **해결 주체**: 코드(경계/음력 회귀 테스트 추가)로 상당 부분 가능. 정책 확정은 자문위원.

---

## P2 — 개선

### P2-1. 합화(合化) 미구현
- `relations.detect_hap_hwa` → NotImplementedError. 학설차로 의도적 보류. **자문위원**.

### P2-2. 스크립트 실행 경로(PYTHONPATH)
- `scripts/spot_check.py`는 `PYTHONPATH=.`로 실행해야 함(`python scripts/...` 단독은 ImportError). 코드로 즉시 해결 가능(P2).

### P2-3. 모바일 미완 화면
- 세운 캘린더·하단 탭(사주/운세/상담/마이)·결제·마이페이지 미작성. 코드로 진행 가능.

---

## 요약

| 우선순위 | 건수 | 자문위원 필요 | 코드로 가능 |
|---|---|---|---|
| P0 | 2 | P0-1, (P0-2 일부) | (P0-2 경계 테스트 일부) |
| P1 | 3 | P1-1, P1-2, (P1-3 정책) | (P1-3 테스트) |
| P2 | 3 | P2-1 | P2-2, P2-3 |

**병목은 코드가 아니라 자문위원·검증 케이스.** 결정론적 엔진은 동작·스팟체크 통과 상태이나,
"검증됨/정통성 확정"으로 올리려면 사람(자문위원)의 케이스·정책 확정이 선결.
